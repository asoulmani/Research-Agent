# ResearchAgent V1

A small AI research assistant for ML / CV papers.

V1 goal: local Python app that:
- reads a folder of documents
- chunks and indexes them
- answers questions using retrieval + an LLM
- returns answers with references to retrieved chunks

This is the initial scaffold. More features coming.
